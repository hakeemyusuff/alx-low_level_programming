0._putchar - This program prints _putchar.

1.alphabet.c - This program prints the alphabet, in lowercase, followed by a new line.

2.-print_alphabet_x10.c - This program prints 10 times the alphabet, in lowercase, followed by a new line.

3.-islower.c - This program checks for lowercase characte and return 1 if true return 0 if otherwise.

4._isalpha.c - This program checks for alphabetic character Returns 1 if c is a letter, lowercase or uppercase Returns 0 otherwise

5.sign.c This program prints the sign of a number Returns 1 and prints + if n is greater than zero, Returns 0 and prints 0 if n is zero, Returns -1 and prints - if n is less than zero.

6._abs - This program computes the absolute value of an integer.

7-print_last_digit.c - Thid program prints the last digit of a number.

8-24_hours.c - This program prints every minute of the day of Jack Bauer, starting from 00:00 to 23:59.

9-times_table.c - This program  prints the 9 times table, starting with 0.