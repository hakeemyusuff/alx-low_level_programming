#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (sucess)
 */

int main(void)
{
	int tens;
	int unit;

	for (tens = '0'; tens <= '9'; tens++)
	{
		for (unit = '1'; unit <= '9'; unit++)
		{
			putchar(tens);
			putchar(unit);
			putchar(',');
			putchar(' ');
		}
	}
}
